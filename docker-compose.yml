version: '3'

services:
  minnemi-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
    depends_on:
      - mysql
    networks:
      - stack_network
    command: composer production-deps-permissions-start-app

networks:
  stack_network:
